# k8s/apps/root/root-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/k8strainer/iac-workload.git
    targetRevision: main
    path: k8s/apps                 # <‑ Verzeichnis mit den beiden Child‑Ordnern
    directory:
      recurse: true                # alle *.yaml Dateien einlesen
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
